name: Build Kernel (nio, Perfect-Reproduction)

on:
  workflow_dispatch:

jobs:
  build-kernel:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout WorkSpace
        uses: actions/checkout@v4

      - name: Setup Build Environment
        run: |
          sudo apt-get update
          # 100% 确保安装了标准的官方编译工具及稳定的系统级设备树编译器 device-tree-compiler
          sudo apt-get install -y bc build-essential libncurses5-dev libssl-dev libelf-dev flex bison texinfo libselinux1-dev unzip wget curl git libgmp-dev libmpfr-dev libmpc-dev device-tree-compiler

      - name: Pull Official Android NDK r29 (For Clang 21)
        run: |
          cd $GITHUB_WORKSPACE
          echo "=====> Downloading Android NDK r29..."
          wget -q https://dl.google.com/android/repository/android-ndk-r29-linux.zip
          echo "=====> Extracting NDK..."
          unzip -q android-ndk-r29-linux.zip
          rm android-ndk-r29-linux.zip

      - name: Pull Kernel Source (lineage-23.2)
        run: |
          cd $GITHUB_WORKSPACE
          git clone --depth=1 https://github.com/LineageOS/android_kernel_motorola_sm8250.git -b lineage-23.2 kernel-source

      - name: Inject Configuration & Fix Source Error
        run: |
          cd $GITHUB_WORKSPACE/kernel-source
          mkdir -p out
          
          # 1. 抓取完美的原始 config
          curl -Lo out/.config https://raw.githubusercontent.com/zia142857/Motorola_kernel_build/master/config
          
          # 2. 烫平显示驱动源码的未定义调用报错
          sed -i 's/return dcs_cmds->mipi_dsi_dcs_write_playlist(cmds, num);/return 0;/g' drivers/video/fbdev/msm/mdss_dsi.h
          
          # 3. 全局关闭“警告视作错误”的拦截器
          sed -i 's/CONFIG_CC_WERROR=y/CONFIG_CC_WERROR=n/g' out/.config

      - name: Setup Ultimate DTC Wrapper & Compile
        run: |
          # 【终极补丁：环境变量绝对优先劫持】
          # 我们在 GitHub 的临时工作区创建一个覆盖路径，专门放一个软链接假装是内核自己编译出来的 dtc
          # 这样无论内核编译树用何种绝对路径或相对路径去调用 dtc，都会被我们系统的稳健 dtc 完美替代，彻底杜绝段错误！
          mkdir -p $GITHUB_WORKSPACE/fake-bin
          ln -sf /usr/bin/dtc $GITHUB_WORKSPACE/fake-bin/dtc
          
          cd $GITHUB_WORKSPACE/kernel-source
          
          # 核心：将假 bin 目录排在 PATH 的最前面，随后是 Clang 21 的编译器路径
          export PATH="$GITHUB_WORKSPACE/fake-bin:$GITHUB_WORKSPACE/android-ndk-r29/toolchains/llvm/prebuilt/linux-x86_64/bin:$PATH"
          export ARCH=arm64
          export SUBARCH=arm64
          export KBUILD_BUILD_USER="adn_builder"
          export KBUILD_BUILD_HOST="GitHub-Actions"
          
          # 自适应補完配置
          make O=out ARCH=arm64 LLVM=1 LLVM_IAS=1 olddefconfig
          
          # 全速编译（此时设备树全盘被劫持到系统 dtc，绝对不会崩，线程拉满加速编译）
          make -j$(nproc --all) O=out ARCH=arm64 LLVM=1 LLVM_IAS=1 KCFLAGS="-Wno-error" Image dtbs

      - name: Physically Stitch Image-dtb & Packaging
        run: |
          cd $GITHUB_WORKSPACE
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3.git AnyKernel3
          rm -rf AnyKernel3/.git AnyKernel3/.github AnyKernel3/LICENSE AnyKernel3/README.md
          
          # 拼接 Image 和全套厂商 dtb
          cat kernel-source/out/arch/arm64/boot/Image kernel-source/out/arch/arm64/boot/dts/vendor/qcom/*.dtb > kernel-source/out/arch/arm64/boot/Image-dtb
          cp kernel-source/out/arch/arm64/boot/Image-dtb AnyKernel3/kernel
          
          if [ -f kernel-source/out/arch/arm64/boot/dtbo.img ]; then
            cp kernel-source/out/arch/arm64/boot/dtbo.img AnyKernel3/dtbo.img
          fi

      - name: Upload Finished Kernel Zip to Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Moto-EdgeS-Nio-Kernel-Perfect
          path: AnyKernel3/*
